
// the bundler is generating this messages based on the json files (configured in vite.config.js)
import messages from '@intlify/unplugin-vue-i18n/messages';

export const defaultTranslationMessages = messages;
